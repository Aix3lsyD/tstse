% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotts_mra.R
\name{plotts_mra}
\alias{plotts_mra}
\title{Plot Multiresolution Analysis}
\usage{
plotts_mra(x, n_levels = 4L, wavelet = "S8", plot = TRUE)
}
\arguments{
\item{x}{Numeric vector. The time series to transform. Length must be
divisible by \eqn{2^{n\_levels}}.}

\item{n_levels}{Integer. Number of decomposition levels. Default is 4.}

\item{wavelet}{Character. Wavelet filter to use. Options are:
\itemize{
\item \code{"S8"} (default): Symmlet 8 (least asymmetric, length 8)
\item \code{"haar"}: Haar wavelet
\item \code{"D4"}: Daubechies 4
\item \code{"D6"}: Daubechies 6
\item \code{"D8"}: Daubechies 8
}}

\item{plot}{Logical. If \code{TRUE} (default), create the multi-panel MRA plot.}
}
\value{
A list with class \code{"plotts_mra"} containing:
\item{mra}{The MRA object from \code{\link[waveslim:mra]{waveslim::mra()}}.}
\item{smooth}{Matrix of cumulative smooth approximations. Row i contains
the smooth approximation S(i-1), where S0 is the original data
reconstructed and SJ is just the smooth coefficients.}
\item{n_levels}{Number of decomposition levels.}
\item{wavelet}{Wavelet filter used.}
\item{n}{Length of the original series.}
}
\description{
Computes and plots the Multiresolution Analysis (MRA) of a time series,
showing cumulative smooth approximations at each decomposition level.
}
\details{
Multiresolution Analysis decomposes a signal into smooth approximations
at different scales. Unlike \code{\link[=plotts_dwt]{plotts_dwt()}} which shows the wavelet
coefficients, MRA shows the cumulative reconstructions:

\itemize{
\item \strong{S0}: Original data (reconstructed from all components)
\item \strong{S1}: S0 minus finest detail (d1 removed)
\item \strong{S2}: S1 minus next detail (d1 and d2 removed)
\item \strong{...}
\item \strong{SJ}: Smooth component only (all details removed)
}

This provides a multi-scale view of the trend, with each successive
smooth showing a coarser approximation.
}
\note{
This function requires the \code{waveslim} package. Install it with
\code{install.packages("waveslim")} if needed.
}
\examples{
\dontrun{
# Generate test signal: trend + seasonal + noise
t <- 1:256
x <- 0.01 * t + 2 * sin(2 * pi * t / 32) + rnorm(256, sd = 0.5)

# Plot MRA with 4 levels
result <- plotts_mra(x, n_levels = 4)

# Use Haar wavelet
result2 <- plotts_mra(x, n_levels = 4, wavelet = "haar")

# Just compute, no plot
result3 <- plotts_mra(x, plot = FALSE)
}

}
\seealso{
\code{\link[=plotts_dwt]{plotts_dwt()}} for the DWT coefficient plot,
\code{\link[waveslim:mra]{waveslim::mra()}} for the underlying MRA computation.
}
