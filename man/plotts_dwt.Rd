% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotts_dwt.R
\name{plotts_dwt}
\alias{plotts_dwt}
\title{Plot Discrete Wavelet Transform}
\usage{
plotts_dwt(x, n_levels = 4L, wavelet = "S8", plot = TRUE)
}
\arguments{
\item{x}{Numeric vector. The time series to transform. Length must be
divisible by \eqn{2^{n\_levels}}.}

\item{n_levels}{Integer. Number of decomposition levels. Default is 4.}

\item{wavelet}{Character. Wavelet filter to use. Options are:
\itemize{
\item \code{"S8"} (default): Symmlet 8 (least asymmetric, length 8)
\item \code{"haar"}: Haar wavelet
\item \code{"D4"}: Daubechies 4
\item \code{"D6"}: Daubechies 6
\item \code{"D8"}: Daubechies 8
}}

\item{plot}{Logical. If \code{TRUE} (default), create the multi-panel DWT plot.}
}
\value{
A list with class \code{"plotts_dwt"} containing:
\item{dwt}{The DWT object from \code{\link[waveslim:dwt]{waveslim::dwt()}}.}
\item{n_levels}{Number of decomposition levels.}
\item{wavelet}{Wavelet filter used.}
\item{n}{Length of the original series.}
}
\description{
Computes and plots the Discrete Wavelet Transform (DWT) of a time series,
displaying the original data, detail coefficients at each level, and the
final smooth coefficients.
}
\details{
The Discrete Wavelet Transform decomposes a time series into components
at different scales:
\itemize{
\item \strong{Detail coefficients (d1, d2, ...)}: Capture fluctuations at
each scale. d1 captures the finest (highest frequency) details,
with coarser details at higher levels.
\item \strong{Smooth coefficients (sJ)}: The remaining low-frequency trend
after J levels of decomposition.
}

The sample size must be divisible by \eqn{2^{n\_levels}} for the
standard DWT with periodic boundary conditions.
}
\note{
This function requires the \code{waveslim} package. Install it with
\code{install.packages("waveslim")} if needed.
}
\examples{
\dontrun{
# Generate test signal: trend + seasonal + noise
t <- 1:256
x <- 0.01 * t + 2 * sin(2 * pi * t / 32) + rnorm(256, sd = 0.5)

# Plot DWT with 4 levels
result <- plotts_dwt(x, n_levels = 4)

# Use Haar wavelet
result2 <- plotts_dwt(x, n_levels = 4, wavelet = "haar")

# Just compute, no plot
result3 <- plotts_dwt(x, plot = FALSE)
}

}
\seealso{
\code{\link[=plotts_mra]{plotts_mra()}} for the multiresolution analysis plot,
\code{\link[waveslim:dwt]{waveslim::dwt()}} for the underlying DWT computation.
}
