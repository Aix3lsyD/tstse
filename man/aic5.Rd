% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aic5.R
\name{aic5}
\alias{aic5}
\title{Display Top 5 ARMA Orders by Information Criterion}
\usage{
aic5(x, p = 0:5, q = 0:2, type = c("aic", "aicc", "bic"), cores = NULL)
}
\arguments{
\item{x}{Numeric vector, the time series.}

\item{p}{Integer vector, AR orders to compare (default 0:5).}

\item{q}{Integer vector, MA orders to compare (default 0:2).}

\item{type}{Character, criterion to use: "aic" (default), "aicc", or "bic".}

\item{cores}{Integer, number of cores for parallel processing.
Default NULL uses \code{getOption("tstse.cores", 1)}.
Set to 0 to use all available cores.}
}
\value{
Invisibly returns a data frame of all (p,q) combinations sorted by criterion.
}
\description{
Fit ARMA models across a grid of orders and display the 5 best by
the chosen information criterion.
}
\details{
This is a convenience function that calls \code{\link{aic_ts}} and
displays the top 5 models. For programmatic use, \code{\link{aic_ts}}
is preferred as it returns the full results.
}
\examples{
x <- gen_arma(n = 200, phi = 0.7, theta = 0.4, plot = FALSE, seed = 123)
aic5(x, p = 0:5, q = 0:2)

\donttest{
# Parallel (uses multiple cores)
aic5(x, p = 0:5, q = 0:3, cores = 2)
}
}
\seealso{
\code{\link{aic_ts}} for the underlying function.
}
