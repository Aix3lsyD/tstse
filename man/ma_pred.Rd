% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ma_pred.R
\name{ma_pred}
\alias{ma_pred}
\title{Moving Average Prediction}
\usage{
ma_pred(x, order = 3L, n_ahead = 1L, plot = TRUE)
}
\arguments{
\item{x}{Numeric vector, the time series.}

\item{order}{Integer, the moving average window size (default 3).}

\item{n_ahead}{Integer, number of steps to forecast beyond the series (default 1).}

\item{plot}{Logical, whether to plot original and smoothed series (default TRUE).}
}
\value{
A list with components:
\item{x_original}{The original time series}
\item{pred}{Smoothed values plus forecasts (length n + n_ahead, first \code{order} values are NA)}
\item{order}{The moving average order used}
}
\description{
Compute a simple moving average and extend forecasts beyond the series.
}
\details{
Computes a simple (backward-looking) moving average where each smoothed value
at time t is the average of the previous \code{order} observations.

For forecasting beyond the series (\code{n_ahead > 1}), predicted values are used
as inputs for subsequent predictions, creating a recursive forecast.

The first \code{order} values of \code{pred} are NA since a full window is required.
}
\examples{
# Smooth a random walk
set.seed(123)
x <- cumsum(rnorm(100))
result <- ma_pred(x, order = 5, n_ahead = 10, plot = FALSE)

# Forecast values are in the last n_ahead positions
tail(result$pred, 10)

}
