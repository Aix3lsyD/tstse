% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen_sigplusnoise.R
\name{gen_sigplusnoise}
\alias{gen_sigplusnoise}
\title{Generate Signal Plus Noise Process}
\usage{
gen_sigplusnoise(
  n,
  b0 = 0,
  b1 = 0,
  coef = c(0, 0),
  freq = c(0, 0),
  psi = c(0, 0),
  phi = 0,
  vara = 1,
  plot = TRUE,
  seed = NULL
)
}
\arguments{
\item{n}{Integer, length of output series.}

\item{b0}{Numeric, intercept (default 0).}

\item{b1}{Numeric, linear trend slope (default 0).}

\item{coef}{Numeric vector, amplitudes for cosine terms (default c(0, 0)).}

\item{freq}{Numeric vector, frequencies for cosine terms (default c(0, 0)).}

\item{psi}{Numeric vector, phase shifts for cosine terms (default c(0, 0)).}

\item{phi}{Numeric vector, AR coefficients for noise (default 0 = white noise).}

\item{vara}{Numeric, noise variance (default 1).}

\item{plot}{Logical, whether to plot the series (default TRUE).}

\item{seed}{Integer, random seed for reproducibility (default NULL).}
}
\value{
Numeric vector of length n.
}
\description{
Generate a realization from a signal plus colored noise model.
}
\details{
Generates from the model:
\deqn{X_t = b_0 + b_1 t + \sum_{j=1}^{k} A_j \cos(2\pi f_j t + \psi_j) + Z_t}
where \eqn{Z_t} is an AR process with coefficients \code{phi} and
innovation variance \code{vara}.
}
\examples{
# Linear trend plus white noise
x <- gen_sigplusnoise(n = 200, b0 = 10, b1 = 0.05)

# Seasonal signal plus AR(1) noise
x <- gen_sigplusnoise(n = 200, coef = c(5, 0), freq = c(1/12, 0),
                      phi = 0.7, seed = 123)

# Two cosine components plus noise
x <- gen_sigplusnoise(n = 200, coef = c(3, 2), freq = c(0.1, 0.25),
                      psi = c(0, pi/4), seed = 456)
}
