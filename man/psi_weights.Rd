% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psi_weights.R
\name{psi_weights}
\alias{psi_weights}
\title{Compute Psi Weights (MA Representation)}
\usage{
psi_weights(phi = 0, theta = 0, lag_max = 5L)
}
\arguments{
\item{phi}{Numeric vector, AR coefficients (default 0 = none).}

\item{theta}{Numeric vector, MA coefficients (default 0 = none).}

\item{lag_max}{Integer, maximum lag for psi weights (default 5).}
}
\value{
Numeric vector of psi weights from lag 1 to lag_max.
}
\description{
Compute the psi weights (coefficients of the infinite MA representation)
for an ARMA model.
}
\details{
The psi weights represent the infinite MA representation of an ARMA process:
\deqn{X_t = a_t + \psi_1 a_{t-1} + \psi_2 a_{t-2} + \cdots}

This is a convenience wrapper around \code{\link[stats]{ARMAtoMA}} that
handles the sign convention used in ATSA.
}
\examples{
# AR(1) psi weights
psi_weights(phi = 0.8, lag_max = 10)

# ARMA(1,1) psi weights
psi_weights(phi = 0.7, theta = 0.4, lag_max = 10)
}
\seealso{
\code{\link{pi_weights}} for the dual (AR) representation.
}
