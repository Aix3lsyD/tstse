% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen_arima.R
\name{gen_arima}
\alias{gen_arima}
\title{Generate ARIMA/SARIMA Process}
\usage{
gen_arima(
  n,
  phi = 0,
  theta = 0,
  d = 0L,
  s = 0L,
  mu = 0,
  vara = 1,
  plot = TRUE,
  seed = NULL,
  spin = 2000L
)
}
\arguments{
\item{n}{Integer, length of output series.}

\item{phi}{Numeric vector, AR coefficients (default 0 = none).}

\item{theta}{Numeric vector, MA coefficients (default 0 = none).}

\item{d}{Integer, order of differencing (default 0).}

\item{s}{Integer, seasonal period (default 0 = non-seasonal).}

\item{mu}{Numeric, theoretical mean (default 0).}

\item{vara}{Numeric, white noise variance (default 1).}

\item{plot}{Logical, whether to plot the series (default TRUE).}

\item{seed}{Integer, random seed for reproducibility (default NULL).}

\item{spin}{Integer, burn-in period (default 2000).}
}
\value{
Numeric vector of length n.
}
\description{
Generate a realization from an ARIMA or seasonal ARIMA process.
}
\examples{
# AR(2) process
x <- gen_arima(n = 200, phi = c(1.6, -0.9))

# ARMA(1,1) with mean 10
x <- gen_arima(n = 200, phi = 0.7, theta = 0.4, mu = 10)

# ARIMA(1,1,1)
x <- gen_arima(n = 200, phi = 0.7, theta = 0.4, d = 1)

# Seasonal (period 12)
x <- gen_arima(n = 200, phi = 0.7, s = 12)
}
