% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unit_circle.R
\name{unit_circle}
\alias{unit_circle}
\title{Plot Roots on the Unit Circle}
\usage{
unit_circle(
  roots = NULL,
  real = 0,
  imaginary = 0,
  main = "Roots and the Unit Circle",
  show_legend = TRUE
)
}
\arguments{
\item{roots}{Complex vector, or two numeric vectors. The roots to plot.
Can be specified as:
\itemize{
\item A complex vector: \code{roots = c(0.5+0.5i, 0.8-0.3i)}
\item Separate real/imaginary: \verb{real = c(0.5, 0.8), imaginary = c(0.5, -0.3)}
}}

\item{real}{Numeric vector. Real parts of roots (used if \code{roots} is NULL).}

\item{imaginary}{Numeric vector. Imaginary parts of roots (used if \code{roots}
is NULL). Default is 0 (real roots).}

\item{main}{Character. Plot title. Default is "Roots and the Unit Circle".}

\item{show_legend}{Logical. If \code{TRUE} (default), show legend indicating
roots inside/outside the unit circle.}
}
\value{
Invisibly returns a data frame with columns:
\item{real}{Real part of each root.}
\item{imaginary}{Imaginary part of each root.}
\item{modulus}{Absolute value (distance from origin).}
\item{inside}{Logical, TRUE if root is inside the unit circle.}
}
\description{
Draws the unit circle and plots polynomial roots, useful for visualizing
the roots of AR or MA characteristic polynomials.
}
\details{
For a stationary AR process, all roots of the AR characteristic polynomial
must lie \strong{outside} the unit circle (equivalently, the reciprocal roots
must lie inside).

For an invertible MA process, all roots of the MA characteristic polynomial
must lie \strong{outside} the unit circle.

Roots on or very close to the unit circle indicate near-nonstationarity
or near-noninvertibility.
}
\examples{
# Plot roots using complex numbers
unit_circle(roots = c(0.5 + 0.5i, 0.5 - 0.5i, 1.2 + 0i))

# Plot roots using real/imaginary parts
unit_circle(real = c(0.5, 0.5, 1.2), imaginary = c(0.5, -0.5, 0))

# Single real root
unit_circle(real = 0.8, imaginary = 0)

# AR(2) example: roots of 1 - 1.5z + 0.75z^2
roots <- polyroot(c(1, -1.5, 0.75))
unit_circle(roots = roots)

}
\seealso{
\code{\link[=factor]{factor()}} for factoring AR/MA polynomials and finding roots.
}
