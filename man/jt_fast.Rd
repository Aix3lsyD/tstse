% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jt_fast.R
\name{jt_fast}
\alias{jt_fast}
\title{Fast Jacob Turner Trend Test with VIF-Adjusted Degrees of Freedom}
\usage{
jt_fast(x, maxp = 5L, type = c("aic", "aicc", "bic"))
}
\arguments{
\item{x}{A numeric vector containing the time series data.}

\item{maxp}{Maximum AR order for model selection. Default is 5.}

\item{type}{Character. Information criterion for order selection:
\code{"aic"}, \code{"aicc"}, or \code{"bic"}. Default is \code{"aic"}.}
}
\value{
A list containing the same elements as \code{\link[=jt]{jt()}}:
\item{x}{The original time series.}
\item{z_x}{Transformed series (cumsum of demeaned differences).}
\item{b0hat}{Estimate of intercept.}
\item{b1hat}{Estimate of slope.}
\item{z_order}{AR order selected for z_x (used for CO transform).}
\item{z_phi}{AR coefficients for z_x.}
\item{x_order}{AR order selected for original x (used for VIF).}
\item{x_phi}{AR coefficients for original x.}
\item{vif}{Variance inflation factor.}
\item{rel_n}{Effective sample size: (n-2)/vif.}
\item{est_df}{Adjusted degrees of freedom.}
\item{pvalue}{P-value using VIF-adjusted degrees of freedom.}
\item{tco}{t-statistic for the slope coefficient.}
}
\description{
Optimized version of \code{\link[=jt]{jt()}} using C++ primitives and vectorized operations.
Produces identical results to \code{\link[=jt]{jt()}} but with significantly better performance
for large-scale simulations.
}
\details{
This function is functionally equivalent to \code{\link[=jt]{jt()}} but uses:
\itemize{
\item C++ Burg algorithm via \code{burg_aic_select_cpp()} instead of \code{aic_burg()}
\item C++ AR transformation via \code{ar_transform_cpp()} instead of \code{artrans()}
\item Vectorized VIF computation instead of R loop
\item \code{lm.fit()} instead of \code{lm()} + \code{summary()} for regression
}

The key performance improvement comes from \code{ar_transform_cpp()} which
performs the AR filtering without computing ACFs (unlike \code{artrans()} which
computes ACFs even when \code{plot = FALSE}).
}
\examples{
# Generate series with trend and AR(1) errors
set.seed(123)
n <- 100
t <- 1:n
z <- arima.sim(list(ar = 0.7), n = n)
x <- 5 + 0.1 * t + z

# Compare jt() and jt_fast()
r1 <- jt(x, maxp = 5)
r2 <- jt_fast(x, maxp = 5)

# Results should be identical
all.equal(r1$tco, r2$tco)
all.equal(r1$pvalue, r2$pvalue)

}
\seealso{
\code{\link[=jt]{jt()}} for the original implementation,
\code{\link[=co]{co()}} for standard Cochrane-Orcutt,
\code{\link[=wbg_boot_fast]{wbg_boot_fast()}} for bootstrap-based trend test
}
