% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_generators.R
\name{make_stat_co}
\alias{make_stat_co}
\title{Create Cochrane-Orcutt Statistic Function}
\usage{
make_stat_co(
  maxp = 5L,
  ar_method = c("burg", "mle"),
  criterion = c("aic", "aicc", "bic")
)
}
\arguments{
\item{maxp}{Integer. Maximum AR order to consider for the error model.
Default is 5.}

\item{ar_method}{Character. Method for AR estimation: \code{"burg"} (default)
or \code{"mle"}.}

\item{criterion}{Character. Information criterion for order selection:
\code{"aic"} (default), \code{"aicc"}, or \code{"bic"}.}
}
\value{
A function that takes a numeric vector \code{x} and returns
the Cochrane-Orcutt t-statistic for the trend coefficient.
}
\description{
Creates a function that computes the Cochrane-Orcutt t-statistic for
testing trend in a time series with autocorrelated errors.
}
\examples{
# Create statistic function
stat_fn <- make_stat_co(maxp = 5)

# Use with bootstrap
set.seed(123)
x <- arima.sim(list(ar = 0.7), n = 100)
stat_fn(x)  # Returns t-statistic

}
\seealso{
\code{\link[=wbg_boot_flex]{wbg_boot_flex()}}, \code{\link[=co]{co()}}
}
