% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expsmooth.R
\name{expsmooth}
\alias{expsmooth}
\title{Simple Exponential Smoothing}
\usage{
expsmooth(x, alpha = NULL, n_ahead = 0L, plot = TRUE)
}
\arguments{
\item{x}{Numeric vector or time series. The input data to smooth.}

\item{alpha}{Numeric scalar between 0 and 1, or \code{NULL}. The smoothing
parameter. Larger values give more weight to recent observations. If
\code{NULL} (default), the optimal value is estimated by minimizing the
squared prediction error.}

\item{n_ahead}{Integer. Number of periods to forecast ahead. Default is 0
(no forecasting, just smoothing).}

\item{plot}{Logical. If \code{TRUE} (default), plot the original series and
smoothed values (and forecasts if \code{n_ahead > 0}).}
}
\value{
A list with class \code{"expsmooth"} containing:
\item{alpha}{The smoothing parameter used (estimated if not supplied).}
\item{fitted}{Numeric vector of smoothed/fitted values for the original
series (length equal to \code{length(x)}).}
\item{forecast}{Numeric vector of forecasted values (length \code{n_ahead}),
or \code{NULL} if \code{n_ahead = 0}.}
\item{hw}{The underlying \link[stats:HoltWinters]{stats::HoltWinters} object for further analysis.}
}
\description{
Performs simple exponential smoothing on a time series with optional
forecasting. This is a convenience wrapper around \code{\link[stats:HoltWinters]{stats::HoltWinters()}}
with \code{beta = FALSE} and \code{gamma = FALSE}.
}
\details{
Simple exponential smoothing computes the smoothed value at time \eqn{t} as:
\deqn{\hat{x}_t = \alpha x_{t-1} + (1 - \alpha) \hat{x}_{t-1}}

This is equivalent to an ARIMA(0,1,1) model. It is appropriate for series
without trend or seasonality.

The smoothing parameter \eqn{\alpha} controls the rate of decay:
\itemize{
\item \eqn{\alpha} close to 1: More weight on recent observations (responsive)
\item \eqn{\alpha} close to 0: More weight on past observations (smooth)
}
}
\examples{
# Smooth a noisy series
set.seed(123)
x <- cumsum(rnorm(100))
result <- expsmooth(x)
result$alpha

# Fixed alpha
result2 <- expsmooth(x, alpha = 0.3)

# With forecasting
result3 <- expsmooth(x, n_ahead = 10)

# No plot
result4 <- expsmooth(x, plot = FALSE)

}
\seealso{
\code{\link[stats:HoltWinters]{stats::HoltWinters()}} for the underlying implementation,
\code{\link[=fore_arma]{fore_arma()}} for ARMA-based forecasting.
}
