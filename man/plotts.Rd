% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotts.R
\name{plotts}
\alias{plotts}
\title{Plot Time Series}
\usage{
plotts(
  x,
  style = 0L,
  xlab = "Time",
  ylab = "",
  main = "",
  col = "black",
  text_size = 12,
  lwd = 0.75,
  cex = 0.5,
  cex.lab = 0.75,
  cex.axis = 0.75,
  xlim = NULL,
  ylim = NULL
)
}
\arguments{
\item{x}{Numeric vector or ts object, the time series.}

\item{style}{Integer, plotting style: 0 = base R (default), 1 = ggplot2.}

\item{xlab}{Character, x-axis label (default "Time").}

\item{ylab}{Character, y-axis label (default "").}

\item{main}{Character, plot title (default "").}

\item{col}{Character, line/point color (default "black").}

\item{text_size}{Numeric, text size for ggplot2 (default 12).}

\item{lwd}{Numeric, line width (default 0.75).}

\item{cex}{Numeric, point size (default 0.5).}

\item{cex.lab}{Numeric, axis label size for base R (default 0.75).}

\item{cex.axis}{Numeric, axis text size for base R (default 0.75).}

\item{xlim}{Numeric vector of length 2, x-axis limits (default NULL).}

\item{ylim}{Numeric vector of length 2, y-axis limits (default NULL).}
}
\value{
For style = 0, returns invisibly. For style = 1, returns a ggplot object.
}
\description{
Plot a time series realization using base R or ggplot2.
}
\details{
For style = 1 (ggplot2), the packages \code{ggplot2} and \code{zoo} must be
installed. If x is a ts object, the x-axis will show dates.

For series with n <= 200, points are shown. For longer series, only lines
are drawn.
}
\examples{
x <- gen_arma(n = 200, phi = 0.9, plot = FALSE, seed = 123)

# Base R plot
plotts(x)

# With title and color
plotts(x, main = "AR(1) Process", col = "blue")

# ggplot2 style (if installed)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  plotts(x, style = 1)
}
}
