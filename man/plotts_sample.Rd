% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotts_sample.R
\name{plotts_sample}
\alias{plotts_sample}
\title{Plot Time Series Sample Analysis}
\usage{
plotts_sample(
  x,
  lag_max = 25L,
  trunc = 0L,
  arlimits = FALSE,
  speclimits = c(0, 0),
  periodogram = FALSE
)
}
\arguments{
\item{x}{Numeric vector, the time series.}

\item{lag_max}{Integer, maximum lag for ACF (default 25).}

\item{trunc}{Integer, Parzen window truncation point. Default 0 uses 2*sqrt(n).}

\item{arlimits}{Logical, whether to show 95\% ACF limits (default FALSE).}

\item{speclimits}{Numeric vector of length 2, y-axis limits for spectral plots.
Default c(0, 0) uses data range.}

\item{periodogram}{Logical, whether to include periodogram plot (default FALSE).}
}
\value{
Invisibly returns a list with components:
\item{xbar}{Sample mean}
\item{acf}{Sample autocorrelations}
\item{freq}{Frequencies for spectral estimates}
\item{parzen_db}{Parzen spectral estimate in dB}
\item{periodogram_db}{Periodogram in dB (if periodogram = TRUE)}
}
\description{
Plot a time series realization with sample autocorrelations, Parzen window
spectral estimate, and optionally the periodogram.
}
\details{
Creates a multi-panel plot showing:
\itemize{
\item Time series realization
\item Sample autocorrelations
\item Parzen window spectral estimate (dB)
\item Periodogram (dB) - if \code{periodogram = TRUE}
}
}
\examples{
x <- gen_arma(n = 200, phi = c(1.5, -0.75), plot = FALSE, seed = 123)

# Basic 3-panel plot
plotts_sample(x)

# With periodogram (4-panel)
plotts_sample(x, periodogram = TRUE)

# With ACF confidence limits
plotts_sample(x, arlimits = TRUE)
}
