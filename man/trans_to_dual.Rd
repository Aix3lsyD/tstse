% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trans_to_dual.R
\name{trans_to_dual}
\alias{trans_to_dual}
\title{Transform to Dual Time Scale}
\usage{
trans_to_dual(x, lambda, offset = 60, h = 0, plot = TRUE)
}
\arguments{
\item{x}{Numeric vector, the time series data.}

\item{lambda}{Numeric, the shape parameter for the transformation.
\itemize{
\item \code{lambda = 0}: Geometric spacing
\item \code{lambda = 1}: Linear spacing (identity-like)
\item Other values: Power-law spacing
}}

\item{offset}{Numeric, shift parameter to avoid singularities at t=0.
Default is 60.}

\item{h}{Numeric, scaling parameter. If <= 1 (for lambda=0) or <= 0
(for lambda != 0), it is computed automatically. Default is 0.}

\item{plot}{Logical, whether to plot the transformed series. Default TRUE.}
}
\value{
A list containing:
\item{int_x}{Dual time points (unequally spaced in original scale)}
\item{int_y}{Interpolated data values on dual scale}
\item{h}{The h parameter used (computed or provided)}
}
\description{
Transform a time series from original (potentially unequally-spaced) time
to an equally-spaced dual time scale using a power-law (G-lambda)
transformation.
}
\details{
The G-lambda transformation maps time according to a power law controlled
by the \code{lambda} parameter. This is useful for:
\itemize{
\item Analyzing data with non-linear sampling patterns
\item Working with astronomical data (e.g., sunspot observations)
\item Preparing data for ARMA modeling on transformed scales
}

The transformation creates an equally-spaced time series in the "dual"
domain by interpolating the original data onto a new time grid.

Use \code{\link[=trans_to_original]{trans_to_original()}} to reverse the transformation.
}
\examples{
# Generate sample data
set.seed(123)
x <- cumsum(rnorm(100))

# Transform with geometric spacing (lambda = 0)
result <- trans_to_dual(x, lambda = 0, offset = 60, plot = FALSE)
length(result$int_y)  # Transformed series length

# Transform with power-law spacing
result <- trans_to_dual(x, lambda = 0.5, offset = 60, plot = FALSE)

}
\references{
Gray, H. L., Zhang, N.-F., & Woodward, W. A. (1989). On generalized
fractional processes. \emph{Journal of Time Series Analysis}, 10(3), 233-257.
}
\seealso{
\code{\link[=trans_to_original]{trans_to_original()}} for the inverse transformation,
\code{\link[=est_glambda]{est_glambda()}} for estimating optimal parameters.
}
