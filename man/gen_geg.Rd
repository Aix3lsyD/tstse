% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen_geg.R
\name{gen_geg}
\alias{gen_geg}
\title{Generate Gegenbauer Process}
\usage{
gen_geg(n, u, d, n_trunc = 1000L, var_a = 1, seed = NULL)
}
\arguments{
\item{n}{Integer. Length of the realization to generate.}

\item{u}{Numeric vector. Gegenbauer frequency parameters. Each value should
be in [-1, 1], determining the spectral peak frequency as
\eqn{f = \arccos(u) / (2\pi)}.}

\item{d}{Numeric vector. Gegenbauer persistence parameters (same length as
\code{u}). Values in (0, 0.5) give stationary long memory. Also known as
\code{lambda} in some references.}

\item{n_trunc}{Integer. Truncation point for the infinite MA representation.
Default is 1000. Larger values give more accurate approximations but
slower computation.}

\item{var_a}{Numeric. Variance of the white noise innovations. Default is 1.}

\item{seed}{Integer or NULL. Random seed for reproducibility. If NULL
(default), no seed is set.}
}
\value{
Numeric vector of length \code{n} containing the generated realization.
}
\description{
Generates a realization of a Gegenbauer (long-memory) process using
the general linear process representation.
}
\details{
A Gegenbauer process is a generalization of ARFIMA that allows long memory
at frequencies other than zero. The spectral density has a pole (peak) at
frequency \eqn{f = \arccos(u) / (2\pi)}.

The process is generated using the general linear process (GLP) form:
\deqn{X_t = \sum_{j=0}^{\infty} \psi_j a_{t-j}}
where \eqn{\psi_j} are the MA (Gegenbauer) coefficients computed by
\code{\link[=macoef_geg]{macoef_geg()}} and \eqn{a_t} is white noise.

Special cases:
\itemize{
\item \code{u = 1}: Equivalent to ARFIMA(0, d, 0) - long memory at frequency 0
\item \code{u = -1}: Long memory at frequency 0.5 (Nyquist)
\item \code{u = 0}: Long memory at frequency 0.25
}
}
\section{Stationarity}{

For stationarity with a single Gegenbauer factor:
\itemize{
\item If \eqn{|u| < 1}: need \eqn{d < 0.5}
\item If \eqn{u = \pm 1}: need \eqn{d < 0.25}
}
}

\examples{
# Gegenbauer process with spectral peak at frequency 0.1
# u = cos(2*pi*0.1) ≈ 0.809
set.seed(123)
x <- gen_geg(n = 200, u = 0.809, d = 0.4)
plot(x, type = "l", main = "Gegenbauer Process")

# Check spectrum - should peak near f = 0.1
parzen(x)

# ARFIMA-like process (u = 1 gives long memory at f = 0)
x2 <- gen_geg(n = 200, u = 1, d = 0.3)

}
\seealso{
\code{\link[=macoef_geg]{macoef_geg()}} for computing the MA coefficients,
\code{\link[=gegenb]{gegenb()}} for Gegenbauer polynomial coefficients,
\code{\link[=gen_arma]{gen_arma()}} for ARMA process generation.
}
