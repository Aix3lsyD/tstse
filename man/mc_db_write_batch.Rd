% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckdb_utils.R
\name{mc_db_write_batch}
\alias{mc_db_write_batch}
\title{Write Batch of Bootstrap Results to Database}
\usage{
mc_db_write_batch(con, results, batch_id, n, phi, innov_dist)
}
\arguments{
\item{con}{A DBI connection from \code{\link[=mc_db_connect]{mc_db_connect()}}.}

\item{results}{A list of \code{wbg_boot_fast} result objects.}

\item{batch_id}{Integer. Batch ID from \code{.mc_create_batch()}.}

\item{n}{Integer. Observation length.}

\item{phi}{Numeric. AR(1) coefficient.}

\item{innov_dist}{Character. Innovation distribution (e.g., "norm", "t(3)").}
}
\value{
Invisible NULL.
}
\description{
Stores multiple bootstrap results in a single transaction.
}
\details{
Extracts fields directly from \code{wbg_boot_fast} output:
\itemize{
\item \code{tco_obs} -> obs_stat
\item \code{boot_tstats} -> boot_dist
\item \code{phi} -> null_ar_phi
\item \code{p} -> null_ar_order
\item \code{vara} -> null_vara
\item p-value fields map 1:1
}
}
\examples{
\dontrun{
con <- mc_db_connect(":memory:")
mc_db_init(con)
batch_id <- .mc_create_batch(con, "test batch")
results <- lapply(1:10, function(i) {
  x <- gen_arma(100, phi = 0.7, plot = FALSE)
  wbg_boot_fast(x, nb = 99, seed = i)
})
mc_db_write_batch(con, results, batch_id, n = 100, phi = 0.7, innov_dist = "norm")
}

}
