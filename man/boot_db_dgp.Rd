% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckdb_utils.R
\name{boot_db_dgp}
\alias{boot_db_dgp}
\title{Create or Get DGP Configuration}
\usage{
boot_db_dgp(
  con,
  n,
  ar_phi = NULL,
  ma_theta = NULL,
  vara = 1,
  innov_dist = "norm",
  innov_params = NULL,
  has_trend = FALSE,
  trend_slope = 0,
  trend_intercept = 0,
  dgp_name = NULL,
  garch_omega = NULL,
  garch_alpha = NULL,
  garch_beta = NULL
)
}
\arguments{
\item{con}{A DBI connection from \code{\link[=boot_db_connect]{boot_db_connect()}}.}

\item{n}{Integer. Series length.}

\item{ar_phi}{Numeric vector. AR coefficients. Default NULL (no AR component).}

\item{ma_theta}{Numeric vector. MA coefficients. Default NULL.}

\item{vara}{Numeric. Innovation variance. Default 1.0.}

\item{innov_dist}{Character. Innovation distribution. Default "norm".}

\item{innov_params}{List. Distribution-specific parameters.}

\item{has_trend}{Logical. Whether series has trend. Default FALSE.}

\item{trend_slope}{Numeric. Trend slope. Default 0.}

\item{trend_intercept}{Numeric. Trend intercept. Default 0.}

\item{dgp_name}{Character. Optional human-readable name.}

\item{garch_omega, garch_alpha, garch_beta}{GARCH parameters if applicable.}
}
\value{
The dgp_id (character UUID).
}
\description{
Creates a new DGP configuration or returns an existing one with matching
parameters. Uses content-based hashing for deduplication.
}
\details{
DGP configurations are deduplicated: if you call this function with the
same parameters twice, you get the same dgp_id back. This prevents
database clutter when running multiple trials with identical DGP settings.
}
\examples{
\dontrun{
con <- boot_db_connect(":memory:")
boot_db_init(con)
dgp_id <- boot_db_dgp(con, n = 100, ar_phi = 0.7, has_trend = FALSE)
}

}
