% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fore_glambda.R
\name{fore_glambda}
\alias{fore_glambda}
\title{Forecast G-Lambda Model}
\usage{
fore_glambda(
  x,
  lambda = 0,
  offset = 60,
  phi = 0,
  n_ahead = 10L,
  lastn = TRUE,
  plot = TRUE
)
}
\arguments{
\item{x}{Numeric vector. The time series to forecast.}

\item{lambda}{Numeric. The G-lambda transformation parameter.
Use \code{lambda = 0} for geometric spacing, positive values for power-law.}

\item{offset}{Numeric. Shift parameter for the transformation. Default is 60.}

\item{phi}{Numeric vector. AR coefficients for the model in dual domain.
Default is 0 (no AR).}

\item{n_ahead}{Integer. Number of steps ahead to forecast. Default is 10.}

\item{lastn}{Logical. If \code{TRUE} (default), use holdout mode: replace the
last \code{n_ahead} observations with forecasts. If \code{FALSE}, forecast
\code{n_ahead} steps into the future.}

\item{plot}{Logical. If \code{TRUE} (default), plot the forecasts.}
}
\value{
An object of class \code{"fore_glambda"} with components:
\item{f}{G-lambda forecasts (length \code{n_ahead})}
\item{ar_f}{AR benchmark forecasts (length \code{n_ahead})}
\item{ar_order}{Order of the AR benchmark model}
\item{original}{Original series}
\item{n_ahead}{Number of steps ahead}
\item{lastn}{Whether holdout mode was used}
}
\description{
Generate forecasts using the G-lambda time transformation method.
Transforms to dual time scale, forecasts in the transformed domain,
then interpolates back to the original scale.
}
\details{
The G-lambda forecasting algorithm:
\enumerate{
\item Transform data to dual time scale using \code{\link[=trans_to_dual]{trans_to_dual()}}
\item Forecast in the dual domain using AR model
\item Re-interpolate forecasts back to original time scale
}

An AR benchmark forecast (using AIC selection) is computed for comparison.
}
\section{Lambda Values}{

\itemize{
\item \code{lambda = 0}: Geometric (exponential) time transformation
\item \code{lambda = 1}: Linear transformation (near identity)
\item \code{lambda > 1}: Power-law with acceleration
}
}

\examples{
# Generate data with time-varying structure
set.seed(123)
x <- gen_glambda(n = 200, lambda = 0.5, phi = 0.7, plot = FALSE)

# Holdout forecast
fit <- fore_glambda(x, lambda = 0.5, offset = 20, phi = 0.7, n_ahead = 10)

# Future forecast
fit2 <- fore_glambda(x, lambda = 0.5, offset = 20, phi = 0.7,
                     n_ahead = 20, lastn = FALSE)

}
\seealso{
\code{\link[=trans_to_dual]{trans_to_dual()}} for the transformation,
\code{\link[=est_glambda]{est_glambda()}} for parameter estimation,
\code{\link[=gen_glambda]{gen_glambda()}} for generating G-lambda processes.
}
