% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckdb_utils.R
\name{boot_db_method}
\alias{boot_db_method}
\title{Create or Get Method Configuration}
\usage{
boot_db_method(
  con,
  method_name,
  nb,
  maxp = 5L,
  ar_method = "burg",
  criterion = "aic",
  bootadj = FALSE,
  stat_fn_name = NULL,
  stat_fn_params = NULL,
  garch_dist = NULL
)
}
\arguments{
\item{con}{A DBI connection from \code{\link[=boot_db_connect]{boot_db_connect()}}.}

\item{method_name}{Character. Name of the bootstrap method.}

\item{nb}{Integer. Number of bootstrap replicates.}

\item{maxp}{Integer. Maximum AR order. Default 5.}

\item{ar_method}{Character. AR estimation method. Default "burg".}

\item{criterion}{Character. Information criterion. Default "aic".}

\item{bootadj}{Logical. Whether COBA adjustment is used. Default FALSE.}

\item{stat_fn_name}{Character. Statistic function name (for wbg_boot_flex).}

\item{stat_fn_params}{List. Parameters for statistic function.}

\item{garch_dist}{Character. GARCH distribution (for wbg_boot_garch).}
}
\value{
The method_id (character UUID).
}
\description{
Creates a new method configuration or returns an existing one with matching
parameters. Uses content-based hashing for deduplication.
}
\details{
Method configurations are deduplicated: if you call this function with the
same parameters twice, you get the same method_id back.
}
\examples{
\dontrun{
con <- boot_db_connect(":memory:")
boot_db_init(con)
method_id <- boot_db_method(con, "wbg_boot", nb = 399, ar_method = "burg")
}

}
