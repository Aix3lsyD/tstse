% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pi_weights.R
\name{pi_weights}
\alias{pi_weights}
\title{Compute Pi Weights (AR Representation)}
\usage{
pi_weights(phi = 0, theta = 0, lag_max = 5L)
}
\arguments{
\item{phi}{Numeric vector, AR coefficients (default 0 = none).}

\item{theta}{Numeric vector, MA coefficients (default 0 = none).}

\item{lag_max}{Integer, maximum lag for pi weights (default 5).}
}
\value{
Numeric vector of pi weights from lag 1 to lag_max.
}
\description{
Compute the pi weights (coefficients of the infinite AR representation)
for an ARMA model.
}
\details{
The pi weights represent the infinite AR representation of an ARMA process:
\deqn{X_t = \pi_1 X_{t-1} + \pi_2 X_{t-2} + \cdots + a_t}

If the \code{astsa} package is installed, uses \code{astsa::ARMAtoAR}.
Otherwise, computes pi weights directly via polynomial inversion.
}
\examples{
# MA(1) pi weights
pi_weights(theta = 0.8, lag_max = 10)

# ARMA(1,1) pi weights
pi_weights(phi = 0.7, theta = 0.4, lag_max = 10)
}
\seealso{
\code{\link{psi_weights}} for the dual (MA) representation.
}
