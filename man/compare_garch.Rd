% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_garch.R
\name{compare_garch}
\alias{compare_garch}
\alias{print.garch_comparison}
\alias{summary.garch_comparison}
\title{Compare Multiple GARCH Model Specifications}
\usage{
compare_garch(
  data,
  arch_range = 0:2,
  garch_range = 0:2,
  distribution = "norm",
  include_mean = FALSE,
  solver = "hybrid",
  parallel = FALSE,
  cores = NULL
)

\method{print}{garch_comparison}(x, ...)

\method{summary}{garch_comparison}(object, ...)
}
\arguments{
\item{data}{Numeric vector of returns or residuals to model.}

\item{arch_range}{Integer vector of ARCH orders to try (default 0:2).}

\item{garch_range}{Integer vector of GARCH orders to try (default 0:2).}

\item{distribution}{Character string specifying the innovation distribution.
One of: "norm", "std", "ged", "snorm", "sged", "sstd", "nig", "ghyp", "jsu".
Default is "norm".}

\item{include_mean}{Logical. Include a mean term in the model? Default is FALSE.}

\item{solver}{Character string for optimization solver. Default is "hybrid"
which tries multiple solvers.}

\item{parallel}{Logical. Use parallel processing for model fitting? Default is TRUE.}

\item{cores}{Integer. Number of cores for parallel processing. If NULL, uses
\code{getOption("tstse.cores")} or \code{detectCores() - 1}.}

\item{x}{A garch_comparison object}

\item{...}{Additional arguments (ignored)}

\item{object}{A garch_comparison object}
}
\value{
A list of class \code{"garch_comparison"} containing:
\describe{
\item{comparison}{Data frame with model statistics}
\item{fits}{Named list of uGARCHfit objects}
\item{distribution}{Distribution used}
\item{n}{Sample size}
\item{call}{The matched call}
}
}
\description{
Fits a grid of ARCH/GARCH models to a time series and computes information
criteria and diagnostic statistics for model comparison and selection.
}
\details{
The function fits all combinations of ARCH and GARCH orders (excluding
the trivial (0,0) case) and computes:
\itemize{
\item AIC, AICc, BIC - information criteria
\item WLB1, WLB2, WLB3 - Weighted Ljung-Box test p-values at different lags
\item Nyblom - stability test statistic and 5\\% critical value
\item SignBias - sign bias test p-value
\item n_sig/n_coef - count of significant coefficients
}

Parallel processing uses \code{mclapply} on Unix/Mac and \code{parLapply}
on Windows. Set \code{parallel = FALSE} to disable.
}
\section{Functions}{
\itemize{
\item \code{print(garch_comparison)}: Print method for garch_comparison objects

\item \code{summary(garch_comparison)}: Summary method for garch_comparison objects

}}
\examples{
\dontrun{
# Generate GARCH data
garch_gen <- make_gen_garch(omega = 0.1, alpha = 0.15, beta = 0.8)
y <- garch_gen(1000)

# Compare models
results <- compare_garch(y)
print(results)

# Display as table
table_garch_gt(results)
table_garch_cli(results)
}
}
\seealso{
\code{\link{table_garch_gt}}, \code{\link{table_garch_cli}} for
display functions.
}
