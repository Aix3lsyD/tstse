% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trans_to_original.R
\name{trans_to_original}
\alias{trans_to_original}
\title{Transform from Dual to Original Time Scale}
\usage{
trans_to_original(xd, lambda, offset, h, plot = TRUE)
}
\arguments{
\item{xd}{Numeric vector, the dual-scale time series data.}

\item{lambda}{Numeric, the shape parameter (must match the value used
in \code{\link[=trans_to_dual]{trans_to_dual()}}).}

\item{offset}{Numeric, shift parameter (must match the value used
in \code{\link[=trans_to_dual]{trans_to_dual()}}).}

\item{h}{Numeric, scaling parameter (must match the value returned
by \code{\link[=trans_to_dual]{trans_to_dual()}}).}

\item{plot}{Logical, whether to plot the transformed series. Default TRUE.}
}
\value{
Numeric vector of interpolated data on the original time scale.
}
\description{
Transform a time series from the dual (equally-spaced) time scale back
to the original time scale using the inverse G-lambda transformation.
}
\details{
This function reverses the transformation performed by \code{\link[=trans_to_dual]{trans_to_dual()}}.
It maps the equally-spaced dual time series back to the original
(potentially unequally-spaced) time scale.

The \code{lambda}, \code{offset}, and \code{h} parameters must match those used
(or returned) by the corresponding \code{\link[=trans_to_dual]{trans_to_dual()}} call.
}
\examples{
# Generate sample data
set.seed(123)
x <- cumsum(rnorm(100))

# Transform to dual scale
dual <- trans_to_dual(x, lambda = 0.5, offset = 60, plot = FALSE)

# Transform back to original scale
original <- trans_to_original(dual$int_y, lambda = 0.5,
                               offset = 60, h = dual$h, plot = FALSE)

}
\seealso{
\code{\link[=trans_to_dual]{trans_to_dual()}} for the forward transformation.
}
