% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen_garch.R
\name{gen_garch}
\alias{gen_garch}
\title{Generate GARCH Process}
\usage{
gen_garch(n, alpha0, alpha, beta, spin = 1000L, plot = TRUE, seed = NULL)
}
\arguments{
\item{n}{Integer, length of output series.}

\item{alpha0}{Numeric, constant term (must be positive).}

\item{alpha}{Numeric vector, ARCH coefficients (q0 terms).}

\item{beta}{Numeric vector, GARCH coefficients (p0 terms).}

\item{spin}{Integer, burn-in period (default 1000).}

\item{plot}{Logical, whether to plot the series (default TRUE).}

\item{seed}{Integer, random seed for reproducibility (default NULL).}
}
\value{
Numeric vector of length n.
}
\description{
Generate a realization from a GARCH(p0, q0) process.
}
\details{
Generates from the model:
\deqn{\sigma^2_t = \alpha_0 + \sum_{i=1}^{q_0} \alpha_i a_{t-i}^2 + \sum_{j=1}^{p_0} \beta_j \sigma^2_{t-j}}
\deqn{a_t = \epsilon_t \sigma_t}
where \eqn{\epsilon_t \sim N(0,1)}.
}
\examples{
# GARCH(1,1)
x <- gen_garch(n = 500, alpha0 = 0.1, alpha = 0.3, beta = 0.5)

# GARCH(2,1)
x <- gen_garch(n = 500, alpha0 = 0.1, alpha = c(0.2, 0.1), beta = 0.6)
}
