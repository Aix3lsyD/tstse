% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fore_garma.R
\name{fore_garma}
\alias{fore_garma}
\title{Forecast GARMA Model}
\usage{
fore_garma(
  x,
  u,
  lambda,
  phi = 0,
  theta = 0,
  n_ahead = 10L,
  lastn = TRUE,
  plot = TRUE
)
}
\arguments{
\item{x}{Numeric vector. The time series to forecast.}

\item{u}{Numeric vector. Gegenbauer frequency parameter(s) (length 1 or 2).}

\item{lambda}{Numeric vector. Gegenbauer persistence parameter(s)
(same length as \code{u}).}

\item{phi}{Numeric vector. AR coefficients (default 0 = no AR).}

\item{theta}{Numeric vector. MA coefficients (default 0 = no MA).}

\item{n_ahead}{Integer. Number of steps ahead to forecast. Default is 10.}

\item{lastn}{Logical. If \code{TRUE} (default), use holdout mode: replace the
last \code{n_ahead} observations with forecasts for validation. If \code{FALSE},
forecast \code{n_ahead} steps into the future.}

\item{plot}{Logical. If \code{TRUE} (default), plot the forecasts.}
}
\value{
An object of class \code{"fore_garma"} with components:
\item{f}{GARMA forecasts (length \code{n_ahead})}
\item{ar_f}{AR benchmark forecasts (length \code{n_ahead})}
\item{ar_order}{Order of the AR benchmark model}
\item{original}{Original series}
\item{n_ahead}{Number of steps ahead}
\item{lastn}{Whether holdout mode was used}
}
\description{
Generate forecasts from an estimated GARMA model using the inverse
Gegenbauer filter transformation.
}
\details{
The forecasting algorithm uses the inverse Gegenbauer filter approach:
\enumerate{
\item \strong{Forward Transform}: Remove Gegenbauer factor(s) by applying
\eqn{(1 - 2uB + B^2)^{\lambda}} to convert the GARMA series to
approximately ARMA.
\item \strong{Forecast}: Generate forecasts in the transformed (ARMA) domain.
\item \strong{Back-Transform}: Reapply the Gegenbauer factor(s) by applying
\eqn{(1 - 2uB + B^2)^{-\lambda}} to return to the original scale.
}

For two-factor models (k = 2), the transformations are applied sequentially.

An AR benchmark forecast is computed for comparison, using AIC to select
the AR order (maximum 8).
}
\section{Holdout vs Future Mode}{

\itemize{
\item \code{lastn = TRUE}: Replaces the last \code{n_ahead} observations with
forecasts, useful for model validation.
\item \code{lastn = FALSE}: Extends the series with \code{n_ahead} future forecasts.
}
}

\examples{
# Generate GARMA data
x <- gen_garma(n = 200, u = 0.8, lambda = 0.3, phi = 0.5,
               plot = FALSE, seed = 123)

# Holdout forecast (validation mode)
fit <- fore_garma(x, u = 0.8, lambda = 0.3, phi = 0.5, n_ahead = 10)

# Future forecast
fit2 <- fore_garma(x, u = 0.8, lambda = 0.3, phi = 0.5,
                   n_ahead = 20, lastn = FALSE)

}
\seealso{
\code{\link[=gen_garma]{gen_garma()}} for generating GARMA realizations,
\code{\link[=est_garma]{est_garma()}} for parameter estimation.
}
