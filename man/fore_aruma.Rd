% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fore_aruma.R
\name{fore_aruma}
\alias{fore_aruma}
\alias{print.fore_aruma}
\title{ARUMA Forecasting}
\usage{
fore_aruma(
  x,
  phi = 0,
  theta = 0,
  d = 0L,
  s = 0L,
  lambda = 0,
  n_ahead = 5L,
  lastn = FALSE,
  plot = TRUE,
  alpha = 0.05,
  limits = TRUE
)

\method{print}{fore_aruma}(x, ...)
}
\arguments{
\item{x}{An object of class "fore_aruma".}

\item{phi}{Numeric vector, AR coefficients (default 0 = none).}

\item{theta}{Numeric vector, MA coefficients (default 0 = none).}

\item{d}{Integer, order of differencing (default 0, max 3).}

\item{s}{Integer, seasonal period (default 0 = none).}

\item{lambda}{Numeric vector, additional AR-type factor coefficients (default 0 = none).}

\item{n_ahead}{Integer, forecast horizon (default 5).}

\item{lastn}{Logical, if TRUE forecast from position n-n_ahead (holdout mode).}

\item{plot}{Logical, whether to plot forecasts (default TRUE).}

\item{alpha}{Numeric, significance level for prediction intervals (default 0.05).}

\item{limits}{Logical, whether to compute/show prediction limits (default TRUE).}

\item{...}{Additional arguments (ignored).}
}
\value{
An object of class "fore_aruma" with components:
\item{f}{Point forecasts (length n_ahead)}
\item{ll}{Lower prediction limits}
\item{ul}{Upper prediction limits}
\item{resid}{Residuals (length n)}
\item{wnv}{Estimated white noise variance}
\item{xbar}{Sample mean of original series}
\item{se}{Standard errors for each forecast horizon}
\item{psi}{Psi weights used for prediction intervals}
}
\description{
Generate point forecasts and prediction intervals for ARUMA models.
ARUMA extends ARIMA with an additional lambda factor for extra AR-type roots.
}
\details{
ARUMA models extend ARIMA by adding an additional AR-type factor (lambda).
The total AR polynomial is: phi(B) * (1-B)^d * (1-B^s) * lambda(B).

Parameters:
\itemize{
\item \code{phi}: AR coefficients for the stationary component
\item \code{theta}: MA coefficients
\item \code{d}: Order of differencing (supports 0, 1, 2, or 3)
\item \code{s}: Seasonal period (e.g., 12 for monthly data)
\item \code{lambda}: Additional AR-type factor (e.g., for extra unit roots)
}
}
\examples{
# Generate data
set.seed(123)
x <- gen_arma(n = 100, phi = c(1.5, -0.75), plot = FALSE)

# ARUMA forecast with lambda factor
fore_aruma(x, phi = 0.8, d = 1, lambda = c(0.5), n_ahead = 5)
}
\seealso{
\code{\link{fore_arima}}, \code{\link{fore_arma}}
}
