% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period.R
\name{period}
\alias{period}
\title{Periodogram}
\usage{
period(x, db = TRUE, plot = TRUE)
}
\arguments{
\item{x}{Numeric vector, the time series.}

\item{db}{Logical, if TRUE (default) return in decibels (10*log10).}

\item{plot}{Logical, if TRUE (default) plot the periodogram.}
}
\value{
A list with components:
\item{freq}{Frequency values (0 to 0.5)}
\item{pgram}{Periodogram values}
}
\description{
Compute and plot the periodogram of a time series.
}
\details{
The periodogram is computed as:
\deqn{I(f) = \hat{\gamma}(0) + 2 \sum_{k=1}^{n-1} \hat{\gamma}(k) \cos(2\pi f k)}
where \eqn{\hat{\gamma}(k)} is the sample autocovariance at lag k.
}
\examples{
x <- gen_arma(n = 200, phi = c(1.5, -0.75), plot = FALSE, seed = 123)

# Default (dB scale)
period(x)

# Raw scale
period(x, db = FALSE)
}
\seealso{
\code{\link{parzen}} for a smoothed spectral estimate.
}
