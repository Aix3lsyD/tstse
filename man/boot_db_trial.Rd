% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckdb_utils.R
\name{boot_db_trial}
\alias{boot_db_trial}
\title{Create a New Trial}
\usage{
boot_db_trial(
  con,
  study_id,
  dgp_id,
  method_id,
  n_planned = NULL,
  trial_name = NULL,
  trial_metadata = NULL
)
}
\arguments{
\item{con}{A DBI connection from \code{\link[=boot_db_connect]{boot_db_connect()}}.}

\item{study_id}{Character. Study ID from \code{\link[=boot_db_study]{boot_db_study()}}.}

\item{dgp_id}{Character. DGP configuration ID from \code{\link[=boot_db_dgp]{boot_db_dgp()}}.}

\item{method_id}{Character. Method configuration ID from \code{\link[=boot_db_method]{boot_db_method()}}.}

\item{n_planned}{Integer. Planned number of simulations. Optional.}

\item{trial_name}{Character. Optional human-readable name for the trial.}

\item{trial_metadata}{List. Optional metadata to attach to the trial.}
}
\value{
The trial_id (character UUID).
}
\description{
Creates a new trial (execution batch) within a study.
}
\details{
A trial represents a single execution batch of simulations. For example,
if you run 1000 simulations today and 1000 more tomorrow with the same
study/DGP/method, those are two separate trials.

Unlike studies, DGP configs, and method configs, trials are NEVER
deduplicated - each call creates a new trial.
}
\examples{
\dontrun{
con <- boot_db_connect(":memory:")
boot_db_init(con)

study_id <- boot_db_study(con, "AR Size Study", "size")
dgp_id <- boot_db_dgp(con, n = 100, ar_phi = 0.7)
method_id <- boot_db_method(con, "wbg_boot", nb = 399)

# First trial
trial1_id <- boot_db_trial(con, study_id, dgp_id, method_id,
                           n_planned = 1000, trial_name = "Trial 1")

# Second trial (same config, different execution)
trial2_id <- boot_db_trial(con, study_id, dgp_id, method_id,
                           n_planned = 1000, trial_name = "Trial 2")
}

}
