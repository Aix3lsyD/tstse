% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/true_farma_acf.R
\name{true_farma_acf}
\alias{true_farma_acf}
\title{True FARMA Autocorrelations}
\usage{
true_farma_acf(
  d = 0,
  phi = 0,
  theta = 0,
  lag_max = 50L,
  trunc = 1000L,
  vara = 1,
  plot = TRUE
)
}
\arguments{
\item{d}{Numeric. Fractional differencing parameter. Typically in (-0.5, 0.5).
Positive values indicate long memory.}

\item{phi}{Numeric vector. AR coefficients (default 0 = none).}

\item{theta}{Numeric vector. MA coefficients (default 0 = none).}

\item{lag_max}{Integer. Maximum lag to compute (default 50).}

\item{trunc}{Integer. Truncation point for infinite sum approximation
(default 1000). Larger values give more accurate results.}

\item{vara}{Numeric. White noise variance (default 1).}

\item{plot}{Logical. If \code{TRUE} (default), plot the ACF.}
}
\value{
A list with components:
\item{acf}{Theoretical autocorrelations (lags 0 to lag_max)}
\item{acv}{Theoretical autocovariances (lags 0 to lag_max)}
}
\description{
Compute and optionally plot the theoretical autocorrelations for a
Fractional ARMA (FARMA) model.
}
\details{
The FARMA(p,d,q) model is defined as:
\deqn{(1-B)^d \phi(B) X_t = \theta(B) a_t}

where \code{d} is the fractional differencing parameter. This is a special case
of the GARMA model with \code{u = 1}.

The theoretical ACF is computed by convolving the ARMA autocovariances
with Gegenbauer polynomial coefficients for the fractional differencing
operator.

Long memory behavior:
\itemize{
\item \code{d > 0}: Long memory (positive dependence)
\item \code{d < 0}: Antipersistence (negative dependence)
\item \code{d = 0}: Reduces to standard ARMA
}
}
\examples{
# Pure fractional difference (FARMA(0,d,0))
true_farma_acf(d = 0.3)

# FARMA(1,d,0) - AR(1) with long memory
true_farma_acf(d = 0.2, phi = 0.5)

# FARMA(0,d,1) - MA(1) with long memory
true_farma_acf(d = 0.25, theta = 0.4)

# When d=0, reduces to standard ARMA
true_farma_acf(d = 0, phi = 0.8, lag_max = 20)

# Without plot
result <- true_farma_acf(d = 0.4, plot = FALSE)

}
\references{
Hosking, J. R. M. (1981). Fractional differencing.
\emph{Biometrika}, 68(1), 165-176.
}
\seealso{
\code{\link[=true_acf]{true_acf()}} for standard ARMA autocorrelations,
\code{\link[=true_garma_acf]{true_garma_acf()}} for the general GARMA case.
}
