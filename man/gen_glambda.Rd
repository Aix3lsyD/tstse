% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen_glambda.R
\name{gen_glambda}
\alias{gen_glambda}
\title{Generate G-Lambda Process Realization}
\usage{
gen_glambda(
  n,
  lambda,
  phi = 0,
  offset = 20,
  sigma2 = 1,
  plot = TRUE,
  seed = NULL
)
}
\arguments{
\item{n}{Integer. Length of the realization to generate.}

\item{lambda}{Numeric. The transformation parameter controlling how the
dual time relates to original time:
\itemize{
\item \code{lambda = 0}: Logarithmic (exponential) transformation
\item \code{lambda = 1}: Linear transformation
\item \code{lambda = 2}: Quadratic transformation
}}

\item{phi}{Numeric vector. AR coefficients (default 0 = white noise).}

\item{offset}{Numeric. Time offset (shift) parameter. Must be positive.
Affects the starting point of the dual transformation. Default 20.}

\item{sigma2}{Numeric. Innovation variance. Default 1.}

\item{plot}{Logical. If \code{TRUE} (default), plot the realization.}

\item{seed}{Integer or NULL. Random seed for reproducibility.
If NULL (default), no seed is set.}
}
\value{
Numeric vector of length \code{n} containing the G-lambda realization.
}
\description{
Generate a realization from a G-lambda process - an AR process on a non-linear
time scale with interpolation back to a regular grid.
}
\details{
The G-lambda process models non-stationary time series by warping a
stationary AR process from a dual time domain back to regular time.
This is useful for modeling processes with time-varying spectral content.

The method:
\enumerate{
\item Computes the h parameter for the given lambda and offset
\item Generates an AR series in the dual time domain
\item Creates a non-uniform time grid in the dual domain
\item Interpolates to equally-spaced original time points
}

Different lambda values produce different non-stationarity patterns:
\itemize{
\item \code{lambda = 0}: Exponentially evolving spectrum
\item \code{lambda = 1}: Linearly evolving spectrum
\item \code{lambda > 1}: Faster than linear evolution
}
}
\examples{
# White noise on G-lambda scale (lambda = 0)
x <- gen_glambda(n = 200, lambda = 0, seed = 123)

# AR(1) on G-lambda scale (lambda = 1)
x <- gen_glambda(n = 200, lambda = 1, phi = 0.9, seed = 123)

# AR(2) with quadratic time warp
x <- gen_glambda(n = 200, lambda = 2, phi = c(1.5, -0.75), seed = 123)

# Without plot
x <- gen_glambda(n = 100, lambda = 0.5, phi = 0.7, plot = FALSE, seed = 42)

}
\references{
Jiang, H., Gray, H. L., & Woodward, W. A. (2006). Time-frequency analysis
of musical rhythm. \emph{Behavior Research Methods}, 38(1), 56-63.
}
\seealso{
\code{\link[=trans_to_dual]{trans_to_dual()}}, \code{\link[=trans_to_original]{trans_to_original()}} for the underlying
transformations, \code{\link[=is_sample]{is_sample()}} for instantaneous spectrum estimation.
}
